Author : Iroiso . I (iroiso@live.com)
Project: Homer SDK
License: Apache License 2.0
Copyright 2011, June inc.


Description:
============
This is the backend for June. I built it for these reasons:
[Put all the reasons why you built homer here]


Example Usage:
==============
from homer.options import *
from homer.core import Model

# Configure Client Options for Memcached
caches = ["localhost:1112", "localhost:1113", "localhost:1114",]
m = CacheOptions(servers=caches, hash="crc32", username="test", password="test")

# Configure Client Options for Cassandra.
instances = ["localhost:9160", "localhost:9161",]
c = DatastoreOptions(servers=instances, username="test", password="test")
c.strategy = NetworkTopologyStrategy(factor = 3, options={"DC1": 1, "DC2": 2})

# Tie them together into a Namespace object; 
namespace = Namespace(name= "June", memcache = m, cassandra= c)

# And tell the API about them.
namespaces.add(namespace)
namespaces.default = "June" # or you could use namespaces.default = namespace

# Now, you can create and ordinary class and store it to Cassandra and Memcache easily
@key("name")
class Profile(Model): 
    name = String("")
    link = URL("http://twitter.com", indexed = True)
    

with Level.Quorum:
    person = Profile(name = 'Yoda', link = 'http://faceboook.com/yoda')
    person.save() # Put in memcache and Cassandra

found = Profile.read('Yoda') # Reads come from cache first.
assert person == found

key = Key("Home", "Profile", "Yoda")
found = db.read(key) # Reads only the static properties of the object from the datastore.

key.columns = ["email", "picture"]
found = db.read(key) # Only fills some members of the Profile object
assert "email", "picture" in found.keys()

# Using CQL to search the index.
found = Profile.query("WHERE link=:link", link="http://facebook.com/iroiso")


Installation:
=============
To install you can use pip or download the latest build from 
[github](http://github.com/june-src/homer)

pip:
====
$ sudo pip install homer

from source:
============
Download the latest build by cloning master repository

$ git clone git://github.com/iroiso/homer.git
$ cd homer
$ sudo python setup.py install


